export enum SocketEventType {
    invalid = "invalid",
    snapshot = "snapshot",
    error_snapshot = "error_snapshot",
    session = "session"
}

export enum RecordEvents {
    ignore = "ignore",
    end = "end"
}